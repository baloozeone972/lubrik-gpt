package com.virtualcompanion.mediaservice.entity;

public enum StreamStatus {
        INITIALIZING,
        CONNECTING,
        CONNECTED,
        ACTIVE,
        RECONNECTING,
        ENDED,
        ERROR
    }
